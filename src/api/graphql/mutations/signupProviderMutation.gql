mutation signupProviderMutation(
  $fullName: String!
  $email: String!
  $password: String!
  $phoneNumber: String!
) {
  signupProvider(
    fullName: $fullName
    email: $email
    password: $password
    phoneNumber: $phoneNumber
  ) {
    message
  }
}
