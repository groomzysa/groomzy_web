mutation resetPasswordMutation(
  $oneTimePin: String!
  $password: String!
  $isProvider: Boolean!
) {
  resetPassword(
    oneTimePin: $oneTimePin
    password: $password
    isProvider: $isProvider
  ) {
    message
  }
}
