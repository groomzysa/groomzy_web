mutation deleteOperatingTimeMutation($dayTimeId: Int!) {
  deleteOperatingTime(dayTimeId: $dayTimeId) {
    message {
      message
    }
    operatingTime {
      id
      day {
        id
        day
      }
      time {
        id
        startTime
        endTime
      }
    }
  }
}
